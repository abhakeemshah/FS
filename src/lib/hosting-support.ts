export function getHostingSupportMessage(domain: string) {
  const host = domain || 'your-site-domain.com';
  return `Hello Hostinger support team,\n\nMy site (${host}) is showing a bot-verification / WAF page for legitimate API requests, which breaks our app sign-in and API calls.\n\nDetails:\n- Affected routes: /api/auth/*  (login POSTs), /api/ledger-state, /api/catalog-state, /api/status\n- Symptom: API POSTs return HTML bot-challenge (content-type: text/html) instead of JSON. Example response contains ReCaptcha/Cloudflare text.\n- Client message: "The hosting provider is blocking the login request with a bot verification page. Please disable bot protection for this site or whitelist /api routes."\n\nRequest:\nPlease disable or relax Bot Protection / WAF for the above API routes, or whitelist them so they are not challenged. Specifically allow POST requests to the following paths:\n\n  - /api/auth/*\n  - /api/ledger-state\n  - /api/catalog-state\n  - /api/status\n\nIf possible, allow the origin https://${host} and requests from typical browser clients.\n\nIf you need any test requests or a specific user-agent to whitelist, let me know and I can provide logs.\n\nThank you,\nSite admin\n`;
}
